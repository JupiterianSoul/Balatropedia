# Dependencies
node_modules/

# Build output (committed to repo so Render free tier doesn't need to build)
# dist/

# SQLite database
*.db
*.db-shm
*.db-wal
*.db-journal
/data/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Temp
/tmp/

# APK project (hidden from the running site; built locally only)
# Note: the /apk directory is checked in (source + config) but its
# build artifacts and synced web assets are gitignored via apk/.gitignore
# Do NOT add /apk to vite's root or import anything from it in /client.
